# Runbook: vault.query / vault.retrieve

1. Search canonical `vault/wiki/` first.
2. Expand via `related` links.
3. Pull raw sources only when canonical notes are missing or low confidence.
4. Return concise answer plus source paths.
5. Prefer canonical note links over re-summarized longform output.
